import ScreenPlaceholder from '../components/ScreenPlaceholder'

function Denials(): React.JSX.Element {
  return (
    <ScreenPlaceholder
      title="Denials"
      description="CARC pareto, by payer, and by root cause (Phase 2)."
    />
  )
}

export default Denials
