import ScreenPlaceholder from '../components/ScreenPlaceholder'

function AR(): React.JSX.Element {
  return (
    <ScreenPlaceholder
      title="A/R"
      description="Aging waterfall, payer vs. patient balances (Phase 2)."
    />
  )
}

export default AR
